name: Mywebsite.js CI

on:
  push:
    branches: [dev]
  pull_request:
    branches: [master]

jobs:
  builds:
    runs-on: self-hosted
    strategy:
      matrix:
        node-version: [16.x]
        os: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          cache-dependency-path: "**/**/package-lock.json"
      - run: npm ci
          pm2 stop 0
          pm2 start 0
          pm2 save
          sudo service nginx restart
        env:
          PORT: 5000
          MONGO_URI: mongodb+srv://heinhtetzaw:20191025hhzslla!@cluster0.13uy3.mongodb.net/?retryWrites=true&w=majority
          JWT_SECRET: 2019102hhzslla!
